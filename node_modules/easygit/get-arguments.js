/*
* 根据输入的key值串，返回命令行的参数对象
*/

var args = process.argv.slice(2);
function getArguments(){
	var obj_args = {};
	[].slice.call(arguments, 0).forEach(function(key, index){
		obj_args[key] = args[index];
	});
	return obj_args;
}
module.exports = getArguments;